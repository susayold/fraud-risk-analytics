(() => { const current=Number(document.body.dataset.currentPart||1); document.querySelectorAll('a[href$=".html"]').forEach(link=>{link.addEventListener('click',event=>{const href=link.getAttribute('href');if(!href||href==='#')return;const match=href.match(/part-(\d+)\.html/);const target=match?Number(match[1]):1;if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;event.preventDefault();document.body.classList.add(target>current?'is-leaving-left':'is-leaving-right');window.setTimeout(()=>{window.location.href=href},720)})}) })();
