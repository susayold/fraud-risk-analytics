# FraudRisk Analytics — Part 1

Static HTML/CSS/JS implementation of the Template 4 plan.

Open `index.html` in a browser or serve this folder with any static web server. The page uses the supplied asset packs, responsive layouts, GSAP/ScrollTrigger reveals, Lenis smooth scrolling, count-up KPI animation, reduced-motion support, tooltips, and the Part 1 → Part 2 transition link.
